## Why

<!-- Explain the motivation for this change. What problem does this solve? Why now? -->

## What Changes

<!-- Describe what will change. Be specific about new members, modified schemas, transformer
     behavior, or removals. Mark breaking changes with **BREAKING**; a member whose shape breaks
     moves to a new apiVersion segment directory (0010 D4). -->

## Before / After

<!-- The CUE shape of every member this change touches, as CUE, not prose. "Before" is "none"
     for a new member. Keep it to the fields that change. This block is the review surface. -->

**Before**

```cue
```

**After**

```cue
```

## Impact

<!-- Downstream consumers reached (modules fleet, subscribing platforms, cli fixtures) and what
     each has to do. Release class (feat: / fix: / feat!:) and whether the v2 alpha line is
     relied on. -->

## Enhancement

<!-- If this implements decisions from enhancements/NNNN, cite the entry and decision numbers
     here and create enhancement.yaml in this change directory. Otherwise write "None." -->
